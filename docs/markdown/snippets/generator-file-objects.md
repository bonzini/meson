## File objects in generators

The `arguments` keyword argument of `generator` and the `process` method of
generator objects now accept also files objects and custom targets (including
single outputs produced with the `[]` operator on a custom target).
