## File objects in generators

The `arguments` keyword argument of `generator`
now accepts also files objects and custom targets (including
single outputs produced with the `[]` operator on a custom target).
